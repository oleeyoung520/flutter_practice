import 'package:flutter/material.dart';

class LoginFormRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center (
        child: Text('LoginFormRoute')
      )
    );
  }
}